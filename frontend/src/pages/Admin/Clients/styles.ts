import styled from 'styled-components';

export const Container = styled.div`
  width: calc(100% - 50px);
  height: 100%;
  overflow-y: auto;
  background-color: var(--white);
  padding: 20px 30px;
`;
